import { Close } from '@icon-park/react';

export default () => {
  return (
    <span className={'flex p-1 rounded-full bg-zinc-600 shadow-md'}>
      <Close theme="outline" size="16" fill="#fff" />
    </span>
  );
};
